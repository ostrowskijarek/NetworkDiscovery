spring:
    application:
        name: Scanner
    cloud:
        consul:
            host: 192.168.1.114
            port: 8500
            discovery:
                instanceId: ${spring.application.name}